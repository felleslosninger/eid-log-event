digdir:
  event:
    logging:
      bootstrap-servers: localhost:443
      schema-registry-url: localhost:443
      kafka-username: franz
      kafka-password: franz123
      schema-registry-password: schemaPassword
      schema-registry-username: schemaUsername
      event-topic: springEventTopic

spring:
  main:
    banner-mode: off
